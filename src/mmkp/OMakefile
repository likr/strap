CxxFiles[] =
	core/item
	core/problem
	algorithm/entropy
	algorithm/enumerate
	algorithm/lp_relaxation
	algorithm/normalize

LIB = $(StaticCXXLibrary libmmkp, $(CxxFiles))

clean:
	rm -f $(LIB)

.SUBDIRS: core algorithm
